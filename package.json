{
  "name": "chordthing",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently 'cd backend && cargo run' 'cd frontend && vp dev'",
    "build": "cd frontend && vp build && cd ../backend && cargo build --release",
    "check": "cd frontend && vp check && cd ../backend && cargo check",
    "test": "cd frontend && vp test && cd ../backend && cargo test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
